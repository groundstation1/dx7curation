{
  "name": "dx7curation",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --strictPort=false",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/",
    "bench": "node test/bench.ts"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
